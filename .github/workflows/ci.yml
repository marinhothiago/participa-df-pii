name: CI - Testes e Cobertura

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]


jobs:
  backend-test-edge-cases:
    name: Backend - Testes Edge Cases
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./backend
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      - name: Instalar dependências
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest pytest-cov codecov
      - name: Baixar modelo spaCy
        run: python -m spacy download pt_core_news_lg
      - name: Rodar testes edge cases
        run: pytest test_edge_cases.py --disable-warnings

  backend-test-regex:
    name: Backend - Testes Regex GDF
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./backend
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      - name: Instalar dependências
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest pytest-cov codecov
      - name: Baixar modelo spaCy
        run: python -m spacy download pt_core_news_lg
      - name: Rodar testes regex
        run: pytest test_regex_gdf.py --disable-warnings

  backend-test-benchmark:
    name: Backend - Testes Benchmark
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./backend
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      - name: Instalar dependências
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest pytest-cov codecov
      - name: Baixar modelo spaCy
        run: python -m spacy download pt_core_news_lg
      - name: Rodar testes benchmark
        run: pytest test_benchmark.py --disable-warnings

  backend-test-amostra:
    name: Backend - Testes Amostra
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./backend
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      - name: Instalar dependências
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest pytest-cov codecov
      - name: Baixar modelo spaCy
        run: python -m spacy download pt_core_news_lg
      - name: Rodar testes amostra
        run: pytest test_amostra.py --disable-warnings

  backend-test-confidence:
    name: Backend - Testes Confiança
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./backend
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      - name: Instalar dependências
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest pytest-cov codecov
      - name: Baixar modelo spaCy
        run: python -m spacy download pt_core_news_lg
      - name: Rodar testes confiança
        run: pytest test_confidence.py --disable-warnings

  backend-test-analise:
    name: Backend - Testes Análise Confiança
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./backend
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      - name: Instalar dependências
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest pytest-cov codecov
      - name: Baixar modelo spaCy
        run: python -m spacy download pt_core_news_lg
      - name: Rodar testes análise confiança
        run: pytest test_analise_confianca.py --disable-warnings

  backend-test-edge-cases:
    name: Backend - Testes Edge Cases
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./backend
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      - name: Instalar dependências
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest pytest-cov codecov
      - name: Baixar modelo spaCy
        run: python -m spacy download pt_core_news_lg
      - name: Rodar testes edge cases
        run: pytest test_edge_cases.py --disable-warnings

  frontend-lint:
    name: Frontend - Lint
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./frontend
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Instalar dependências
        run: npm install

      - name: Rodar lint
        run: npm run lint